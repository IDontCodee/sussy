
this.__sussy$config = {
    usingPort: () => { if(window.location.port) { return true } else { return false } },
    sus_server: '/not-sus-server/',
    _co: '/co/gateway?url=',
    _rho: '/rho/gateway?url=',
    moreStuffURI: "example.com"
}